<div class = "container">

  <div class = "row mt-3 mb-3">

    <h5 class = "mark">新規会員登録</h5>

  </div>

  <div class = "row mt-3 mb-3">

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "public/shared/error_messages", resource: resource %>

  <table class = "table  table-sm table-borderless">

    <tr><td><%= f.label :名前 %></td><td>
    <%= f.label :（姓） %>
    <%= f.text_field :last_name, autocomplete: "last_name" %>

    <%= f.label :（名） %>
    <%= f.text_field :first_name, autocomplete: "first_name" %></td></tr>

    <tr><td><%= f.label :フリガナ %></td><td>
    <%= f.label :（セイ） %>
    <%= f.text_field :last_name_kana, autocomplete: "last_name_kana" %>

    <%= f.label :（メイ） %>
    <%= f.text_field :first_name_kana, autocomplete: "first_name_kana" %></td></tr>

    <tr><td><%= f.label :メールアドレス %></td><td>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %></td></tr>

    <tr><td><%= f.label :郵便番号（ハイフンなし） %></td><td>
    <%= f.text_field :postal_code, autocomplete: "postal_code" %></td></tr>

    <tr><td><%= f.label :住所 %></td><td>
    <%= f.text_field :address, autocomplete: "address",size: "70×35" %></td></tr>

    <tr><td><%= f.label :電話番号（ハイフンなし） %></td><td>
    <%= f.text_field :telephone_number, autocomplete: "telephone_number" %></td></tr>

    <tr><td><%= f.label :パスワード %></td><td>
    <% if @minimum_password_length %>
    <em>（<%= @minimum_password_length %>文字以上）</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "new-password" %>

    <tr><td><%= f.label :パスワード（確認用）%></td><td>
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %></td></tr>

  </table>

    <div class = "row mt-3 mb-3">&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;

      <div class="col">
      <%= f.submit "新規登録",class:"btn btn-success" %>

      <% end %>

      </div>

      <div>既に登録済みの方</div>

      <div><%= render "public/shared/links" %>からログインを行ってください。</div>


    </div>

  </div>

</div>